/**
 * Friends Feature Module
 *
 * Contains friend-related components, hooks, and types
 */

// Hooks
export { useFriends, useSearchUsers, useAddFriend, useUserProfile } from '../../hooks/useFriends'

// Components (re-exported from existing locations)
export { FriendList } from '../../components/FriendList'

// Types
export type { UserProfile } from '../../types'
